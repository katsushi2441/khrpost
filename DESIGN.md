# Kurage HR Post（khrpost）設計書

**職（ポジション）を主役にした、職務・職責・目標・タスクの管理。**

- 名称: Kurage HR Post ／ フォルダ: `khrpost`
- 形態: 1ファイルPHP + SQLite（レンタルサーバーで動く。買い切りで Kurage App Store に出品）
- 社員マスタ: 持たない。外部の `employees.json` を読むだけ（kvgwc か、自作のJSON）
- 作成: 2026-08-20 ／ 最終更新: 2026-08-21

---

## 1. 何を解決するか

人事システムの多くは「人」を主語にするため、**その職に何が求められているか**が人に紐づいて属人化する。担当者が代わると、何をする職なのかが失われる。

本システムは主語を**職（Post）**に置く。

- 職は**人がいなくても存在する**（空席も管理対象）
- 職に「職務・職責・目標」がぶら下がる
- 人は**任命**によって職に就く
- タスクは職に紐づいて蓄積される。だから**後任は前任の記録をそのまま読める**

> 設計原則: 「まずロール（職）、次に人」。

## 2. スコープ

**やること**
- 職の定義（名称・概要・必要人数）
- 任命と空席の管理、歴代の担当者
- 職務・職責・目標の登録（常に3つ一緒）
- タスクの記録（やること／やったこと）
- 達成率と等級の自動計算

**やらないこと**（別システムの領域。混ぜない）
- 社員マスタの管理（kvgwc または自前のJSONが正）
- 給与計算・勤怠打刻（勤怠は Kurage Kintai）
- 採用管理、予算・ヘッドカウント計画、組織図の編集

**あえて作らなかったもの**（複雑さの割に価値が出ないと判断した）
- 評価期間（期）… どの期間に誰が任命されていたかは `assignments` の日付で足りる
- 引き継ぎパッケージの生成 … 前任の記録が読めることが引き継ぎの実体
- 職務・職責・目標の重み付け … 単純平均で足りる
- 監査ログ … 記録者は tasks に残る

## 3. データモデル

テーブルは4つだけ。社員は持たない。

| テーブル | 主な列 |
|---|---|
| `posts` 職 | id, name, memo, headcount, active, created_at |
| `assignments` 任命 | id, post_id, emp_key, from_date, to_date, created_at |
| `duties` 職務・職責・目標 | id, post_id, job_memo, resp_memo, goal_memo, target_value, unit, created_at |
| `tasks` タスク | id, post_id, duty_id, done_on, todo, done, emp_key, created_at |

- 職務・職責・目標は 1:1:1 なので **1行に3つ持つ**。別テーブルに割らない
- `emp_key` は `kv:` + employees.json の `id`。氏名は保存せず、表示のたびにマスタから引く
- タスクは `post_id` に紐づく。担当者が代わっても職の履歴として残る

**導出値（保存しない）**
- 職の充足状態 ＝ 有効な任命の件数 vs headcount → 空席 / 充足
- 達成率・職スコア・等級

> 保存値にしない理由: 経過日数やフラグを保存すると必ず実態とズレる（CRMでの教訓）。

## 4. 達成度の計算（決定的・AIに採点させない）

```
実績     = その目標のタスクのうち「やったこと」が書かれている件数
達成率   = 実績 / 目標値        （上限 KHP_RATE_CAP、既定120%。目標値0は対象外）
職スコア = 目標値のある目標の達成率の単純平均
等級     = KHP_GRADE_TABLE による変換（既定 S:110 / A:100 / B:85 / C:70 / 下回ればD）
```

計算はコードが確定的に行う。AIは下書き（タスクの起票）まで。

## 5. 権限（宣言関門 `khp_can($actor, $action)`）

| ロール | できること |
|---|---|
| staff（担当者） | **任命された自分の職**について、職務・職責・目標とタスクの登録・編集・削除 |
| admin（管理者） | 上記に加えて、職の管理と任命 |
| ai（連携・任意） | **タスクの下書き作成のみ** |

判定は `khp_can()` 1か所に集約する（kvgwc の `kv_can()` と同じ思想）。画面側で権限を散らさない。
さらに `khp_assert_post()` が「その職の担当者本人か」を見るので、担当者は他人の職を触れない。

## 6. 社員マスタ（khrpost の外）

`KHP_EMPLOYEES_DIR` に `employees.json` があるディレクトリを指定する。無ければ設置手順の画面を出す。

```json
{"employees":[{"id":"e1","no":"E001","name":"山田 太郎","status":"active"}]}
```

- `id` … 社員を一意に識別する文字列。任命と記録者がこれで紐づく（変えない）
- `no` … 社員コード。担当者ログインのID
- `status` … `active` 以外はログインできない。**退職者もマスタから消さない**（消すと過去の記録の記録者名が出なくなる）

用意のしかたは2通り。

1. **kvgwc を使う** … `kvgwc_data` のパスを指定するだけ。退職処理は kvgwc 側の status 変更だけで khrpost のログインも止まる（判定を持ち込まない）
2. **kvgwc を使わない** … 同じ形式のJSONを自分で置く（既存台帳やCSVからの書き出しはバイブコーディングで作れる）

部署は扱わない。部署は人事側で見る情報で、職の管理には要らない。

## 7. 画面（5つ）

1. **ホーム** — 職の一覧。空席の職を警告表示
2. **職** — 職務・職責・目標の一覧と登録・編集・削除。歴代の担当者から前任の記録を開ける
3. **タスク** — その目標の「やること／やったこと」の一覧と登録・編集・削除
4. **任命** — 管理者のみ。いつから誰がその職か、任命の終了と履歴
5. ログアウト

**入力を減らす仕組み**: 登録フォームは「入力」と「これまでの記録からコピー」のタブ。参照タブに前任を含む過去の記録が並び、「コピー」で入力欄へ転記される。入力が面倒だと誰も使わなくなるので、ここは削らない。

## 8. 品質・出荷条件（Kurage App Store 出品）

- 自己テスト `scripts/check_khrpost.php` が全通過すること。`deploy_demo.sh` は失敗したら公開しない
- テストには「構造の単純さ」の機械チェックを含める（テーブルが4つを超えない、社員テーブルを作らない、画面が5つを超えない、重み列を持たない等）。**勝手に複雑化しないための歯止め**
- `khrpost_config.php.example` を同梱し、実値は含めない
- デモ: `proto.exbridge.jp/khrpost/`（実在の個人情報は入れない）
- 価格: 買い切り 55,000円（税込）
- 配布物: `public/`（本体・アセット・設定雛形）、`scripts/`、`README.md`

## 9. 個人情報の扱い

- 評価に関わる情報を扱う。閲覧は関門1か所で制御する
- 退職者のデータは**消さない**（記録の証跡）。ログインだけが止まる
- データディレクトリには `Deny from all` の `.htaccess` を自動生成する。公開ディレクトリの外に置ければなお良い
